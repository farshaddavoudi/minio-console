name: Deploy React Frontend (Self-Hosted)

on:
  push:
    branches: [ata]
    paths:
      - 'web-app/**'

jobs:
  build-and-deploy:
    runs-on: self-hosted
    defaults:
      run:
        shell: bash

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install dependencies and build React
      working-directory: ./web-app
      run: |
        yarn install --immutable  # Equivalent to `npm ci`
        CI='' yarn build  # Unset CI (behave warnigns as error)

    - name: Deploy to web server directory
      run: |
        sudo rm -rf /var/www/web-app/*
        sudo cp -r ./web-app/build/* /var/www/minio-console/
